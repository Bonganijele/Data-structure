With the simple code above, you can use a generic tree to list subdirectories in a tree format. You can also use a linked list—a linear data structure where elements (nodes) are linked together via pointers



Please use export bash first to lacate directories:

 # Linux/macOs
  export="/home/yourpcname/music"

# Windows
set PATH_TO_DIR=C:\Users\YourPCName\Music


